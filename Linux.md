```
服务器

前台部署是静态资源部署，使用了nginx, apache,或者tomcat等工具
```

```
获取树形目录结构：

brew install tree

tree -C
颜色显示
tree -f
显示文件全路径
tree -L 2
只显示2层
tree -P *.pl
只显示文件目录和*.pl的perl文件。
tree -F
显示目录后面的\；显示可执行文件*；功能类似ls -F
tree –help
帮助手册。ps：linux所有命令，都可以用--help去扩展思路。
总结
tree -FC应该是最最常用的。
```